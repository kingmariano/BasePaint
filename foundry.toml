[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.15"
optimizer = true
optimizer_runs = 200
evm_version = "london"

[rpc_endpoints]
base = "${BASE_RPC_URL}"

[etherscan]
base = { key = "${BASESCAN_API_KEY}", url = "https://api.basescan.org/api" }
